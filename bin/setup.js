#!/usr/bin/env node

/**
 * 智能任务管理器 - 安装脚本
 * 跨平台安装脚本（Node.js）
 */

const fs = require('fs');
const path = require('path');
const os = require('os');

// 颜色输出（跨平台）
const colors = {
  reset: '\x1b[0m',
  green: '\x1b[32m',
  yellow: '\x1b[33m',
  red: '\x1b[31m',
  blue: '\x1b[34m',
};

console.log(`${colors.blue}🧠 智能任务管理器 - 安装脚本${colors.reset}`);
console.log('================================');

// 获取 OpenClaw 目录
const openclawDir = process.env.OPENCLAW_DIR || path.join(os.homedir(), '.openclaw');
const workspaceDir = path.join(openclawDir, 'workspace');

console.log('');
console.log(`📁 OpenClaw 目录: ${openclawDir}`);
console.log(`📁 工作区目录: ${workspaceDir}`);

// 检查目录
if (!fs.existsSync(openclawDir)) {
  console.error(`${colors.red}❌ 错误：OpenClaw 目录不存在${colors.reset}`);
  console.log('   请先安装 OpenClaw');
  process.exit(1);
}

// 创建工作区目录
if (!fs.existsSync(workspaceDir)) {
  console.log('');
  console.log('📦 创建工作区目录...');
  fs.mkdirSync(workspaceDir, { recursive: true });
  console.log(`${colors.green}✅ 工作区目录已创建${colors.reset}`);
}

// 配置文件内容
const configFiles = {
  '任务类型映射表.md': `# 任务类型映射表

> **⚠️ 每次执行任务前必须查阅此表**

---

## ⚡ 快速通道（优先检查）

**快速通道包括：**
- 信息查询类（查文件、查信息、查文档、查历史、查命令）
- 计算类（简单计算、单位转换、时间计算、数据统计）
- 生成类（小段代码、短文本、简单配置、格式化）
- 辅助类（解释说明、翻译、总结、校对）
- 娱乐类（说笑话、闲聊、测试）

---

## 📋 任务类型对照

| 任务类型 | 触发词 | 流程文档 | 状态 |
|---------|--------|---------|:----:|
| **公众号写作** | 写文章、改写、润色、公众号、选题 | （待创建）| ❌ |
| **配图** | 配图、生成图片、封面图 | （待创建）| ❌ |
| **代码开发** | 写代码、开发、调试、编程 | （待创建）| ❌ |
| **数据分析** | 分析、统计、报表、数据 | （待创建）| ❌ |
| **文档整理** | 整理文档、总结、归纳 | （待创建）| ❌ |
| **服务器管理** | 服务器、部署、运维 | （待创建）| ❌ |
| **其他任务** | （未匹配的任务）| 通用任务执行流程.md | ✅ |

---

*使用智能任务管理器自动创建和管理流程文档*
`,

  '快速通道任务列表.md': `# 快速通道任务列表

> **⚡ 以下任务可以直接执行，不需要走完整流程**

---

## 🎯 快速通道任务

### 信息查询类（秒级任务）

| 任务 | 说明 | 示例 |
|------|------|------|
| **查文件** | 查找本地文件 | "帮我找 xxx 文件" |
| **查信息** | 查询已知信息 | "Python 的版本是什么？" |
| **查文档** | 读取文档内容 | "看一下 xxx 文档" |
| **查历史** | 查看历史记录 | "上次是怎么做的？" |
| **查命令** | 查询命令用法 | "git 怎么回退？" |

---

### 计算类（秒级任务）

| 任务 | 说明 | 示例 |
|------|------|------|
| **简单计算** | 基础数学计算 | "123 + 456 = ?" |
| **单位转换** | 单位换算 | "1MB 等于多少 KB？" |
| **时间计算** | 日期时间计算 | "3 天后是几号？" |
| **数据统计** | 简单统计 | "这组数的平均值是多少？" |

---

### 生成类（分钟级任务）

| 任务 | 说明 | 示例 |
|------|------|------|
| **生成代码片段** | 小段代码（< 20 行）| "写个快速排序" |
| **生成文本** | 短文本（< 200 字）| "写个请假条" |
| **生成配置** | 简单配置文件 | "写个 .gitignore" |
| **格式化** | 代码/文本格式化 | "格式化这段 JSON" |

---

### 辅助类（秒级任务）

| 任务 | 说明 | 示例 |
|------|------|------|
| **解释说明** | 解释概念/术语 | "什么是闭包？" |
| **翻译** | 文本翻译 | "把这段翻译成英文" |
| **总结** | 简单总结 | "总结一下这段话" |
| **校对** | 文本校对 | "检查这段代码的语法" |

---

### 娱乐类（秒级任务）

| 任务 | 说明 | 示例 |
|------|------|------|
| **说笑话** | 讲个笑话 | "说个笑话" |
| **闲聊** | 简单对话 | "你好" |
| **测试** | 功能测试 | "测试一下" |

---

*使用智能任务管理器自动管理快速通道*
`,

  '通用任务执行流程.md': `# 通用任务执行流程

> **适用于没有专门流程文档的任务**

---

## 第0步：确认任务

| 项目 | 内容 |
|------|------|
| 任务目标 | 具体要做什么？ |
| 截止时间 | 什么时候要？ |
| 交付形式 | 文件？消息？图片？ |

---

## 第1步：信息收集

- 搜索记忆：memory_search "关键词"
- 查找文档
- 确认信息源

---

## 第2步：制定方案

提供 2 个方案让用户选择

---

## 第3步：执行

按选定方案执行，关键节点确认

---

## 第4步：验收

展示结果，确认满意度

---

*使用智能任务管理器自动执行任务*
`,

  '流程文档模板.md': `# 流程文档模板

> **⚠️ 创建新的任务流程文档时，使用此模板确保格式统一**

---

## 📋 文档头部

# [任务名称]流程文档

> **任务类型：** [开发/写作/分析/管理/其他]
> **难度级别：** [入门/中等/高级]
> **预计耗时：** [X 分钟/X 小时]
> **创建日期：** [YYYY-MM-DD]
> **版本：** [v1.0]

---

## 🎯 任务概述

### 任务说明
简要描述这个任务是什么

### 适用人员
- 适合谁使用？
- 需要什么基础？

### 前置条件
- [ ] 条件 1
- [ ] 条件 2

---

## 📝 完整流程

### 第0步：任务确认

| 项目 | 内容 |
|------|------|
| **任务目标** | 具体要做什么？ |
| **交付形式** | 文件？代码？文档？ |

---

### 第1步：[步骤名称]

说明：这个步骤要做什么

**操作：**
1. [ ] 操作 1
2. [ ] 操作 2

---

## 🔧 工具和资源

| 工具 | 用途 | 是否必需 |
|------|------|:--------:|
| 工具 1 | 说明 | ✅ |

---

## ⚠️ 注意事项

### 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| 错误 1 | 原因 | 解决方法 |

---

## ✅ 检查清单

- [ ] 确认任务目标
- [ ] 结果符合要求
- [ ] 记录经验教训

---

*使用智能任务管理器创建和管理流程文档*
`
};

// 复制配置文件
console.log('');
console.log('📦 复制配置文件...');

Object.entries(configFiles).forEach(([filename, content]) => {
  const filePath = path.join(workspaceDir, filename);

  if (fs.existsSync(filePath)) {
    console.log(`${colors.yellow}⚠️  ${filename} 已存在，跳过${colors.reset}`);
  } else {
    fs.writeFileSync(filePath, content, 'utf8');
    console.log(`${colors.green}✅ ${filename} 已创建${colors.reset}`);
  }
});

// 完成
console.log('');
console.log('================================');
console.log(`${colors.green}✅ 安装完成！${colors.reset}`);
console.log('');
console.log('📋 下一步：');
console.log('   1. 重启网关：openclaw gateway restart');
console.log('   2. 开始新对话：/new');
console.log('   3. 测试 Skill：你好');
console.log('');
console.log('📚 文档位置：');
console.log(`   - ${workspaceDir}/任务类型映射表.md`);
console.log(`   - ${workspaceDir}/快速通道任务列表.md`);
console.log(`   - ${workspaceDir}/通用任务执行流程.md`);
console.log(`   - ${workspaceDir}/流程文档模板.md`);
console.log('');
console.log('✨ 享受智能任务管理吧！');
console.log('================================');
